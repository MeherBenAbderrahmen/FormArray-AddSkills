import { PcntVentePipe } from './pcnt-vente.pipe';

describe('PcntVentePipe', () => {
  it('create an instance', () => {
    const pipe = new PcntVentePipe();
    expect(pipe).toBeTruthy();
  });
});
